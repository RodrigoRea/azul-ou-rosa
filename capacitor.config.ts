import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.mybabeis3app',
  appName: 'Azul ou Rosa',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
